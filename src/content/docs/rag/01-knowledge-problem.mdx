---
title: "Module 1: The Knowledge Problem"
description: "Why LLMs fail at enterprise knowledge, what RAG actually solves, and when to use it over fine-tuning."
---

import DeepDive from '../../../components/DeepDive.astro';
import ColabLink from '../../../components/ColabLink.astro';
import ModuleHeader from '../../../components/ModuleHeader.astro';
import PresenterNote from '../../../components/PresenterNote.astro';
import KeyInsight from '../../../components/KeyInsight.astro';
import FlowDiagram from '../../../components/FlowDiagram.astro';
import { Tabs, TabItem, Steps, Aside, Card, CardGrid } from '@astrojs/starlight/components';

<ModuleHeader duration="40 min" day={1} goal="Understand why RAG exists, what it solves, and when to choose it over fine-tuning." />

## Demo First

Before definitions, let's watch RAG work.

<PresenterNote>
**Ekranda:** Colab — [3.1-rag-pipeline.ipynb](https://colab.research.google.com/github/kuthaygumus/build-with-ai/blob/main/public/notebooks/rag/3.1-rag-pipeline.ipynb)
**Yapın:** İlk 2 hücreyi çalıştırın: biri plain LLM'e, diğeri RAG-powered sisteme aynı soruyu sorar. İki çıktıyı yan yana gösterin. Henüz açıklamayın — kontrastı görmelerini sağlayın.
**Sorun:** "İki cevap arasındaki fark ne? Neden biri kaynak gösteriyor, diğeri göstermiyor?"
**Sonra:** Notebook'ta DURUN. Site'a dönün — "Why LLMs Fail" bölümüne geçin.
</PresenterNote>

Dün güncellenen bir internal şirket politika dokümanınız var. Plain LLM'e sorun:

```text
Q: What is the company's remote work policy for contractors?
A: I don't have specific information about your company's internal policies...
```

Şimdi aynı soruyu RAG'lı bir sisteme sorun:

```text
Q: What is the company's remote work policy for contractors?
A: According to the Contractor Policy (v3.2),
   contractors may work remotely up to 3 days per week, subject to
   manager approval and compliance with local labor regulations.
   [Source: contractor-policy-v3.2.pdf, page 14]
```

Aynı model. Aynı soru. Fark: birinin dokümanlarınıza erişimi var. Diğerinin yok.

**RAG bu.** Daha akıllı bir model değil — daha akıllı bir mimari.

<KeyInsight>
RAG gives LLMs access to knowledge they were never trained on. The model doesn't change. The input does.
</KeyInsight>

## Why LLMs Fail at Enterprise Knowledge

LLM'lerin üç temel kör noktası var. Her biri enterprise kullanım için engelleyici. RAG üçünü de çözer.

### 1. Hallucination

LLM'ler bir şeyleri "bilmez". Sonraki token'ı tahmin eder. Eğitim verisi bir konuda seyrekse, boşluğu inandırıcı görünen kurguyla doldurur.

```python
# Ask about a specific Flight API version
response = llm("What are the parameters for the FlightOffers v4 API?")
# The LLM will confidently describe parameters that don't exist,
# mixing real v2 params with invented ones.
# It doesn't know the difference. It can't.
```

<PresenterNote>
**Ekranda:** Colab — [3.1-rag-pipeline.ipynb](https://colab.research.google.com/github/kuthaygumus/build-with-ai/blob/main/public/notebooks/rag/3.1-rag-pipeline.ipynb)
**Yapın:** 'Hallucination Demo' hücresini çalıştırın. Var olmayan bir internal API hakkında soru sorun — LLM'in güvenle uydurduğunu gösterin. Sonra RAG versiyonunu çalıştırın — aynı soru, gerçek dokümana dayalı cevap.
**Sorun:** "Bunu production'a deploy eder misiniz?"
**Sonra:** Notebook'ta DURUN. Site'a dönün — Stale Knowledge bölümüne geçin.
</PresenterNote>

### 2. Stale Knowledge

Her modelin bir eğitim kesim tarihi var. Dünya ilerlemeye devam eder. Model etmez.

Internal dokümanlarınız her gün değişir. Yeniden eğitim GPU-haftaları ve altı haneli rakamlar demek. Bu bir workflow değil — kriz.

### 3. Zero Access to Private Data

LLM'ler açık internet üzerinde eğitilir. Internal wiki'leriniz, müşteri sözleşmeleriniz, GDS dokümantasyonunuz, incident raporlarınız veya mimari kararlarınız hakkında **sıfır bilgiye** sahiptir.

Bu bir model kalitesi problemi değil. GPT-5 çözmeyecek. Claude 5 çözmeyecek. **Mimari problemi.** RAG mimari çözüm.

<FlowDiagram title="The Three Blind Spots">
  <div class="flow-row">
    <div class="flow-node danger">
      <span class="node-icon">🎭</span>
      <span class="node-label">Hallucination</span>
      <span class="node-desc">Invents plausible fiction</span>
    </div>
    <div class="flow-arrow">→</div>
    <div class="flow-node danger">
      <span class="node-icon">📅</span>
      <span class="node-label">Stale Knowledge</span>
      <span class="node-desc">Training cutoff is months old</span>
    </div>
    <div class="flow-arrow">→</div>
    <div class="flow-node danger">
      <span class="node-icon">🔒</span>
      <span class="node-label">No Private Data</span>
      <span class="node-desc">Can't see your documents</span>
    </div>
  </div>
  <div class="flow-row" style="justify-content: center; margin-top: 0.5rem;">
    <div class="flow-arrow animated" style="font-size: 0.85rem; color: var(--sl-color-gray-4);">↓ RAG solves all three</div>
  </div>
  <div class="flow-row" style="justify-content: center;">
    <div class="flow-node success">
      <span class="node-icon">📚</span>
      <span class="node-label">RAG</span>
      <span class="node-desc">Ground answers in real documents</span>
    </div>
  </div>
</FlowDiagram>

## What Is RAG — 30 Saniyede

**Retrieval-Augmented Generation.** Modelin eğitimden "hatırladığına" güvenmek yerine, ilgili dokümanları alıp prompt'a enjekte edersiniz.

Model dokümanlarınızı okur, sonra cevaplar. Her seferinde. Taze veri. Kaynaklı cevaplar.

<FlowDiagram title="The RAG Flow">
  <div class="flow-row">
    <div class="flow-node secondary">
      <span class="node-icon">❓</span>
      <span class="node-label">Question</span>
      <span class="node-desc">User asks something</span>
    </div>
    <div class="flow-arrow animated">→</div>
    <div class="flow-node secondary">
      <span class="node-icon">🔍</span>
      <span class="node-label">Retrieve</span>
      <span class="node-desc">Find relevant chunks</span>
    </div>
    <div class="flow-arrow animated">→</div>
    <div class="flow-node secondary">
      <span class="node-icon">📄</span>
      <span class="node-label">Augment</span>
      <span class="node-desc">Inject docs into prompt</span>
    </div>
    <div class="flow-arrow animated">→</div>
    <div class="flow-node success">
      <span class="node-icon">✅</span>
      <span class="node-label">Generate</span>
      <span class="node-desc">Grounded answer + citations</span>
    </div>
  </div>
</FlowDiagram>

İşin özü şu: **LLM tüm bilgi tabanınızı asla görmez.** Sadece en ilgili birkaç parçayı görür, soruyla birlikte enjekte edilmiş olarak.

```python
# RAG in 10 lines of pseudocode
def rag_answer(question: str) -> str:
    # 1. Find relevant documents
    relevant_docs = vector_store.similarity_search(question, k=5)

    # 2. Build prompt with context
    context = "\n\n".join([doc.page_content for doc in relevant_docs])
    prompt = f"""Answer based ONLY on this context. If unsure, say so.

Context:
{context}

Question: {question}"""

    # 3. Generate grounded answer
    return llm.invoke(prompt)
```

<PresenterNote>
**Ekranda:** Site — RAG pseudocode
**Yapın:** Kodu satır satır geçin. Vurgulayın: LLM aynı, sihir 2. satırdaki similarity_search'te. 1-2 tahmin alın. "Module 2'de bunu derinlemesine öğreneceğiz."
**Sorun:** "similarity_search arka planda ne yapıyor sizce?"
**Sonra:** Site'ta devam — RAG vs Fine-Tuning bölümüne geçin.
</PresenterNote>

## RAG vs Fine-Tuning

Herkesin sorduğu soru. Karar şöyle:

| | RAG | Fine-Tuning |
|---|---|---|
| **Purpose** | Model needs to **know** something | Model needs to **behave** differently |
| **Data freshness** | Real-time (re-index anytime) | Stale (requires retraining) |
| **Source attribution** | Built-in (you know which doc) | Not possible |
| **Cost** | Low (embedding + storage) | High (GPU hours) |
| **Hallucination control** | Strong (grounded in sources) | Weak (still generates freely) |
| **Best for** | Knowledge retrieval, Q&A, search | Tone, format, domain language |

<KeyInsight>
Use RAG when you need the model to **know** something. Use fine-tuning when you need the model to **behave** differently. In production, you often use both.
</KeyInsight>

<FlowDiagram title="When to Use What">
  <div class="flow-row">
    <div class="flow-node secondary">
      <span class="node-icon">❓</span>
      <span class="node-label">Need specific docs?</span>
      <span class="node-desc">Policies, APIs, contracts</span>
    </div>
    <div class="flow-arrow animated">→</div>
    <div class="flow-node success">
      <span class="node-icon">📚</span>
      <span class="node-label">RAG</span>
      <span class="node-desc">Retrieve and ground</span>
    </div>
  </div>
  <div class="flow-row" style="margin-top: 0.5rem;">
    <div class="flow-node secondary">
      <span class="node-icon">❓</span>
      <span class="node-label">Need different tone?</span>
      <span class="node-desc">Style, format, domain jargon</span>
    </div>
    <div class="flow-arrow animated">→</div>
    <div class="flow-node warning">
      <span class="node-icon">🎯</span>
      <span class="node-label">Fine-Tuning</span>
      <span class="node-desc">Train behavior into model</span>
    </div>
  </div>
  <div class="flow-row" style="margin-top: 0.5rem;">
    <div class="flow-node secondary">
      <span class="node-icon">❓</span>
      <span class="node-label">Neither?</span>
      <span class="node-desc">General capability is enough</span>
    </div>
    <div class="flow-arrow animated">→</div>
    <div class="flow-node secondary">
      <span class="node-icon">💬</span>
      <span class="node-label">Prompt Engineering</span>
      <span class="node-desc">Just write a better prompt</span>
    </div>
  </div>
</FlowDiagram>

## Real Use Cases — Sizin Dünyanız

Bir seyahat teknolojisi şirketinde zaten mevcut olan veriyi düşünün:

<CardGrid>
  <Card title="GDS Documentation" icon="document">
    Thousands of pages of Cryptic commands, API specs, integration guides. Developers searching through them daily. RAG makes them queryable in natural language.
  </Card>
  <Card title="Customer Support" icon="comment">
    Support agents get AI-suggested answers grounded in product docs, troubleshooting guides, and past ticket resolutions. Faster resolution, fewer escalations.
  </Card>
  <Card title="Incident Post-Mortems" icon="warning">
    Past outages, root causes, remediation steps. When something breaks at 3 AM, the on-call engineer asks: "Has this happened before?" RAG finds the answer.
  </Card>
  <Card title="Internal Wikis" icon="open-book">
    Engineering standards, architecture decisions, onboarding docs. New hires ask questions, RAG answers from the wiki — with page references.
  </Card>
</CardGrid>

<PresenterNote>
**Ekranda:** Site — Use Cases kartları
**Yapın:** 2-3 cevap alın. Bunlar eğitimin geri kalanı için zihinsel çapalar olacak. Module 3'te [3.1-rag-pipeline.ipynb](https://colab.research.google.com/github/kuthaygumus/build-with-ai/blob/main/public/notebooks/rag/3.1-rag-pipeline.ipynb) notebook'unda bu use case'lerden birini end-to-end göstereceğinizi söyleyin.
**Sorun:** "Bu use case'lerden hangisi takımınıza en çok zaman kazandırır?"
**Sonra:** Site'ta devam — Indexing Phase bölümüne geçin.
</PresenterNote>

## The Indexing Phase — Sorgudan Önce Ne Olur

RAG herhangi bir şeyi cevaplayabilmeden önce bilgi tabanınızı hazırlarsınız. Bu offline olur, bir kez:

<FlowDiagram title="Offline Indexing Pipeline">
  <div class="flow-row">
    <div class="flow-node secondary">
      <span class="node-icon">📄</span>
      <span class="node-label">Load</span>
      <span class="node-desc">PDF, web, markdown, API</span>
    </div>
    <div class="flow-arrow animated">→</div>
    <div class="flow-node secondary">
      <span class="node-icon">✂️</span>
      <span class="node-label">Chunk</span>
      <span class="node-desc">Split into focused passages</span>
    </div>
    <div class="flow-arrow animated">→</div>
    <div class="flow-node secondary">
      <span class="node-icon">🧮</span>
      <span class="node-label">Embed</span>
      <span class="node-desc">Convert text to vectors</span>
    </div>
    <div class="flow-arrow animated">→</div>
    <div class="flow-node success">
      <span class="node-icon">💾</span>
      <span class="node-label">Store</span>
      <span class="node-desc">Save in vector database</span>
    </div>
  </div>
</FlowDiagram>

```python
from langchain_community.document_loaders import PyPDFLoader
from langchain.text_splitter import RecursiveCharacterTextSplitter
from langchain_openai import OpenAIEmbeddings
from langchain_chroma import Chroma

# 1. Load
docs = PyPDFLoader("flight-api-guide.pdf").load()

# 2. Chunk
chunks = RecursiveCharacterTextSplitter(
    chunk_size=1000, chunk_overlap=200
).split_documents(docs)

# 3. Embed + Store
vector_store = Chroma.from_documents(
    documents=chunks,
    embedding=OpenAIEmbeddings(model="text-embedding-3-small"),
    persist_directory="./chroma_db"
)

print(f"Indexed {len(chunks)} chunks from {len(docs)} pages")
```

Her adımı sonraki modüllerde derinlemesine işleyeceğiz. Şimdilik akışı hatırlayın: **Load, Chunk, Embed, Store.**

## Sırada Ne Var

Bu modül size **neden**'i verdi. Geri kalanı **nasıl**'ı verecek:

| Module | Topic | What You Build |
|--------|-------|----------------|
| 2 | Embeddings & Vector World | Embed text, search semantically, visualize meaning |
| 3 | The RAG Pipeline | Complete end-to-end RAG system from scratch |
| 4 | Advanced RAG | HyDE, re-ranking, agentic RAG |
| 5 | Production RAG | Evaluation, scaling, monitoring, grand finale |

<DeepDive title="The Academic Origins of RAG">

RAG was introduced in the 2020 paper **"Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks"** by Patrick Lewis et al. at Facebook AI Research.

The key contribution: combine a pre-trained seq2seq model (BART) with a dense retriever (DPR), trained end-to-end. The retriever and generator jointly optimize what to retrieve and how to generate.

Modern RAG has diverged significantly. Today's systems use frozen LLMs (no end-to-end training), vector databases instead of FAISS, and prompt engineering instead of fine-tuned generators. The term "RAG" has evolved from a specific architecture to a **design pattern** — any system that retrieves external knowledge to augment generation.

**Key papers in the RAG lineage:**
1. **DPR** (Karpukhin et al., 2020) — Dense Passage Retrieval
2. **RAG** (Lewis et al., 2020) — The original paper
3. **RETRO** (Borgeaud et al., 2022) — Retrieval-enhanced transformers at scale
4. **Self-RAG** (Asai et al., 2023) — LLM decides when to retrieve
5. **CRAG** (Yan et al., 2024) — Corrective RAG with retrieval evaluation

</DeepDive>

## Özet

Üç şey:

1. **LLM'lerin üç kör noktası var** — hallucination, eski bilgi, private veriye erişim yok. RAG üçünü de düzeltir — modeli değiştirerek değil, girdiyi değiştirerek.

2. **RAG = al, sonra üret.** İlgili dokümanları bul, prompt'a enjekte et, modelin kanıttan cevap vermesini sağla. Basit konsept, güçlü sonuçlar.

3. **RAG vs fine-tuning rekabet değil.** RAG bilgi için. Fine-tuning davranış için. Hangi problemi çözdüğünüzü bilin.

## Next Up

İlk pratik soru: "similarity_search" aslında nasıl çalışır? Metni anlam taşıyan sayılara nasıl dönüştürürsünüz? **Module 2: Embeddings & The Vector World.**
